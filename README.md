# Yolo8m MNIST Digit Detector

A YOLOv8 model trained on a custom version of the MNIST dataset with annotations for detection of handwritten digits 0–9.

## How to Start

```bash
pip install -r requirements.txt
```

